<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme without ActionBar chrome. Avoid windowFullscreen here — it can
         interact poorly with Compose AndroidView focus/key dispatch on Android TV. -->
    <style name="Theme.EliteIPTVDVR" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
